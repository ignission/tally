#!/bin/bash
# =============================================================================
# Stop Gate フック
# Lefthook (pre-commit/pre-push) でテストが実行されるため、
# Stop hookでのテスト実行は重複。即座にexit 0で通過する。
# =============================================================================

exit 0
